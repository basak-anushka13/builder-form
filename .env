# MongoDB connection string (optional - falls back to local storage if not provided)
# MONGODB_URI=mongodb://localhost:27017/formcraft

# Development environment
NODE_ENV=development

# Server port (optional - defaults to 3001 in development, 3000 in production)
# PORT=3001
